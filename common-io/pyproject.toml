[project]
name = "common-io"
version = "0.1.0"
description = "Common I/O utilities for the NPUCompute project."
authors = [
    { name = "lbroglio", email = "lukebroglio@gmail.com" }
]
requires-python = ">=3.14"
dependencies = []

[project.scripts]
common-io = "common_io:main"

[build-system]
requires = ["uv_build>=0.12.7,<0.13.0"]
build-backend = "uv_build"
